<< Este programa es testing de estructuras sintacticas validas
Puede encontarse ademas algun error lexico y como
se combina con el error sintactico >>
programa1 {

    f64 a, b, d <<falta ;>>
    ui16 c, e, f, g, h <<falta ;>>
    const i 3.2, j =: 2 <<falta ; y simbolo asignacion>>
    
   	c =: -5000 <<se va a poner en 0>>
   	b =: -1.2250738585072014D-308 <<se va a truncar al minimo permitido>>
    
    <<if mal definido>>
    if (a!=b) then
    	a =: b
    end_if
    
    <<if mal definido>>
    if (a!=b) then {
    	a =: b
    end_if
    
    <<mas de 2 parametros>>
    fun f1(ui16 h, ui16 i, ui16 p, f64 w): ui16 {
    	return (h)
    }
    
    <<no hay sentencias en el cuerpo de la funcion>>
    fun f3 () : f64
    {}
    
    <<header mal definido>>
    fun f2 ) : f64
    {
        return (z);
    }

    <<falta la condicion en el until>>
    do {
    	a =: b;
    } until();
    
    <<falta el id o cadena en el out>>
    out();
}