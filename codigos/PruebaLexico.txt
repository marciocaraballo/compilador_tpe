{ 
    PRINT % Consultas %,
    
     VOID fun() {
     
		DO {
			a = b,
		} WHILE (a>b),    
	     
        IF (a > b) {
            a = b,
            
            IF (a > b) 
            	RETURN,
            END_IF,
            
            DO
				RETURN,
			WHILE (a>b), 
            
            RETURN,
        } ELSE {
            a = b,
            RETURN,
        }
        END_IF,

        RETURN,
    }
}