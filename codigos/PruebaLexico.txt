*{ esto es 

IF

un comment }*

{ 
    IF ( a > b ) {
    	a = b,
    	a = b,
    	a = b,
    	
    	IF ( b == d )
    		a = b,
    	END_IF,
    	
    } ELSE {
    	a = b,
    	a = b,
    	a = b,
    }
    END_IF,
}