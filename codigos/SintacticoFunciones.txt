<< Este programa es testing de estructuras sintacticas
tanto validas como invalidas >>
programa1 {

	f64 a, b, d;
	ui16 c, e, f, g, h;
	
	if (a >=b) then 
		b =: c;
	end_if;
	
	fun f3 () : f64
	{
		f64 x;
		x =: 1.2;
		if (x >= 0) then 
			return (z);
		else 
			return (z);
		end_if;
	}

	fun f3 () : f64
	{
		f64 x;
		x =: 1.2;
		if (x >= 0) then 
			a =: b;
		end_if;

		return (z);
	}
	
	a =: f3();
	c =: f1(e);
	f =: f2(g, h) + f1(c);
}
